[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "skillloader"
version = "0.1.1"
description = "SkillLoader MCP launcher"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
skillloader = "skillloader_launcher:main"
